let borderWidth = 0
let borderHeight = 0

function setup() {
    createCanvas(windowWidth, windowHeight)
    borderWidth = windowWidth
    borderHeight = windowHeight
}

function draw() {
    background("#ffdfff")
    drawColorfulBoxes()
}
